{
  "name": "sortee",
  "version": "1.0.0",
  "description": "Library to allow you to sort your Tealium iQ extensions and tags automatically",
  "source": "src/sortee.ts",
  "targets": {
    "default": {
      "includeNodeModules": [
        "lodash"
      ]
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build",
    "watch": "parcel watch",
    "prettier": "prettier --write src/**/*.ts",
    "prettier-watch": "onchange 'src/**/*.ts' -- prettier --write {{changed}}"
  },
  "author": "Adam Olczak <adam@olczak.xyz>",
  "license": "ISC",
  "devDependencies": {
    "@parcel/packager-ts": "^2.6.0",
    "@parcel/transformer-typescript-types": "^2.6.0",
    "@types/lodash": "^4.14.182",
    "onchange": "^7.1.0",
    "parcel": "^2.6.0",
    "prettier": "^2.6.2",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
